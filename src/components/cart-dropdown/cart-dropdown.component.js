import React from "react";
import Button from "../button/Button";
import "./cart-dropdown.styles.scss";

const CartDropdown = () => {
  return (
    <div className="cart-dropdown">
      <div className="cart-items">s</div>
      <Button>GO TO CHECKOUT</Button>
    </div>
  );
};

export default CartDropdown;
